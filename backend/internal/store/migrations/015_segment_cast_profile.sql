-- 015_segment_cast_profile.sql
-- Adds cast_profile_id to script_segments for cast-profile-based voice resolution.
-- The actual ALTER TABLE is applied programmatically via addColumnIfMissing.
SELECT 1;
