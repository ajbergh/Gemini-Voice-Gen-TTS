services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - app-data:/home/app/data
    environment:
      - GEMINI_PASSPHRASE=${GEMINI_PASSPHRASE:-}
    restart: unless-stopped

volumes:
  app-data:
